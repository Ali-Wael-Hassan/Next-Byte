// This question will be replaced for some issues
#include <iostream>

using namespace std;

int main() {
    // define 32-bit integer variable
    int i;
    // take the 32-bit integer from user
    cin >> i;
    // printing the 32-bit integer
    cout << i << '\n';
    // define 64-bit integer variable
    long long ll;
    // take the 64-bit integer from user
    cin >> ll;
    // printing the 32-bit integer
    cout << ll << '\n';
    // define character variable
    char ch;
    // take character from user
    cin >> ch;
    // printing the character
    cout << ch << '\n';
    // define 32-bit real number variable
    float f;
    // take the 32-bit real number from user
    cin >> f;
    // printing the 32-bit real number
    cout << f << '\n';
    // define 64-bit real number variable
    double d;
    // take the 64-bit real number from user
    cin >> d;
    // printing the 64-bit real number
    cout << d << '\n';
    
    return 0;
}