#include <iostream>

using namespace std;

// start with alphabetic char or _
// you can't use special chars except _
// no spaces

int main() {
    // int x = 2.9999;
    // cout << x << '\n';
    // float y = 3.5;
    // cout << y << '\n';
    // char c = 'd';
    // cout << c << '\n';
    // bool isTrue = 0;
    // cout << isTrue << '\n';

    // Byte = 8 bits
    // short(2 Bytes) -> int(4 Bytes) -> long(4 Bytes) -> long long(8 Bytes)
    // min value = -2^(n-1), max value = 2^(n-1) - 1
    // unsigned short(2 Bytes) -> int(4 Bytes) -> long(4 Bytes) -> long long(8 Bytes)
    // min value = 0, max value = 2^(n) - 1
    // signed = int

    // float (4 Bytes) -> double (8 Bytes) -> long double (12->16 Bytes)

    // bool(1bit)

    // char(1 Byte)

    // string (number of chars Bytes)

    // string name;
    // cin >> name;
    // cout << "Hello, " << name << "!\n";

    // int x, y;
    // cin >> x >> y;

    // cout << x - y << '\n';

    // 1-> int (or any integer type)
    // 1/2 = 0
    // 2-> float (or any other decimal type)
    // 1.0f/2 = 0.5

    // int x, y;
    // cin >> x >> y;
    // float x1 = x, y1 = y;
    // cout << x / y << '\n' << x1/y1 << '\n';

    // 10 mod 2 = 0
    // 10 mod 4 = 2

    // int x, y;
    // cin >> x >> y;
    // cout << x%y << '\n';

    // () -> *,/,% -> +,-

    // cout << 5+3*2 << '\n';
    // cout << (5+3)*2 << '\n';
    // cout << 2*3*3 << '\n';
    // cout << (4+6)/2-1 << '\n';
    // cout << 10-4/2 << '\n';

    // int x = 1;
    // x += 3;// x = x + 3
    // cout << x << '\n';

    // int x = 0;
    // cout << x++ << '\n'; // post increment = evaluate first then add 1
    // cout << x << '\n';
    // int y = 0;
    // cout << ++y << '\n'; // pre increment = add 1 then evaluate
    // cout << y << '\n';

    // Syntax error : wrong in writing names
    // Logical error : code is right but does wrong algorithm
    // run time error : do something illegal

    // unsigned long long a, b;
    // cin >> a >> b;
    // cout << a*a + b*b << '\n';

    // int a, b;
    // cin >> a >> b;
    // cout << a+b << 1 << '\n';

    // return 0;
}