#include <iostream>

using namespace std;

int main() {
    // take input
    int a, b; cin >> a >> b;
    // print a+b then 1 so in console will appear as the problem wants
    cout << a+b << 1 << '\n';
    // can be done also
    // cout << 10*(a+b)+1 << '\n';
    
    return 0;
}